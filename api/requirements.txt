# Dependencies for the Vercel serverless function.
#
# Deliberately excludes scikit-learn, numpy, scipy, pandas and pyarrow: together
# they are ~370MB unpacked against a 250MB function limit. The serving path was
# restructured so none of them are needed -- feature medians and corpus
# aggregates are precomputed at training time into small JSON files, and the
# predictor falls back to closed-form economics when the model cannot be loaded.
#
# The full set for local development and training lives in backend/requirements.txt.
fastapi>=0.115
pydantic>=2.7
httpx>=0.27
supabase>=2.20
