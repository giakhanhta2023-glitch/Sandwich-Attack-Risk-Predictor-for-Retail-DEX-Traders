# Public values only: safe to commit.
#
# Loaded with the lowest priority: any real environment variable, and any .env
# file, overrides these. They let every deployment read the live research tables
# without configuration. The publishable key is limited by row level security to
# SELECT on public tables; nothing grants it a write. The service-role key never
# belongs here.
SUPABASE_URL=https://zhmgaubrpkbooabztajo.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_nNswer1CYTrKANVLaB_rjQ_f9AxVEsn
